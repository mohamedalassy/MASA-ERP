# ZATCA — الفاتورة الإلكترونية (المرحلة الثانية)

## إيه اللي كان موجود عندك

البنية كانت **مجهّزة صح**: عمود `uuid` و`zatca_status` على الفاتورة،
علاقة `ZatcaDocument`، وكل حقول المشتري العنوانية اللي الهيئة بتطلبها
(رقم المبنى، الشارع، الحي، الرمز البريدي). اللي كان ناقص هو **التنفيذ بالكامل**.

## الملفات

| الملف | الغرض |
|---|---|
| `Zatca/ZatcaQrGenerator.php` | ترميز TLV بالوسوم التسعة |
| `Zatca/ZatcaHashService.php` | هاش الفاتورة + سلسلة PIH + التحقق منها |
| `Zatca/ZatcaInvoiceXmlBuilder.php` | بناء UBL 2.1 بـ DOMDocument |
| `Zatca/ZatcaSigningService.php` | التوقيع XAdES-BES |
| `Zatca/ZatcaClient.php` | نداءات المقاصة والإبلاغ والتوافق |
| `Zatca/ZatcaSubmissionService.php` | منسّق الدورة كاملة |
| `ZatcaController.php` | ٧ مسارات |

## الدورة

```
إصدار الفاتورة
   ↓
بناء UBL 2.1 XML
   ↓
هاش SHA-256 على XML مقنَّن (بدون التوقيع والـ QR)
   ↓
توقيع ECDSA + توليد QR بالوسوم التسعة
   ↓
قياسية B2B → مقاصة (مزامن، قبل التسليم للعميل)
مبسطة B2C → إبلاغ (خلال ٢٤ ساعة)
   ↓
تسجيل الردّ + تحديث zatca_status
```

## نقاط حرجة

### ١. رمز QR في المقاصة

في الفاتورة القياسية، **الهيئة بترجع رمز QR جاهزًا** مع ردّ المقاصة.
الكود بيستخدم اللي بترجعه الهيئة ويستبدل المحلي:

```php
'qr_code' => $response['qr'] ?: $prepared->qr_code,
```

اللي بتطبعه على الفاتورة لازم يكون بتاع الهيئة.

### ٢. طول TLV بالبايت مش بالحرف

أكثر سبب لرفض الرموز: استخدام `mb_strlen` بدل `strlen`. الاسم العربي
بيأخذ بايتين لكل حرف في UTF-8، فالطول يطلع غلط والرمز يبان سليم
بصريًا ويفشل عند التحقق. الكود بيستخدم `strlen` وبيدعم الطول الممتد
(`0x82`) للقيم الأكبر من ٢٥٥ بايت زي التوقيع والمفتاح العام.

### ٣. سلسلة الهاش (PIH)

أشهر خطأ عند التكامل: «هاش الفاتورة السابقة مفقود أو غير صحيح». أسبابه:

- أول فاتورة بدون قيمة PIH الابتدائية الصحيحة
- ترتيب الفواتير بـ `created_at` بدل `counter_value`
- إعادة توليد الهاش بعد تعديل الفاتورة

الكود بيحجز الرقم التسلسلي داخل ترانزاكشن بـ `lockForUpdate`، فمستحيل
فاتورتين متزامنتين ياخدا نفس الرقم. ومعاه أداة تشخيص:

```
GET /api/finance/zatca/verify-chain
```

بترجع بالظبط فين السلسلة اتكسرت وإيه القيمة المتوقعة مقابل المحفوظة.

### ٤. استبعاد عناصر من الهاش

ثلاثة عناصر تُستبعد من حساب الهاش:
`ext:UBLExtensions` · `AdditionalDocumentReference[ID=QR]` · `cac:Signature`

وده منفّذ في `ZatcaHashService::canonicalize()`.

### ٥. أكواد الفئة الضريبية

`S` قياسي · `Z` صفري · `E` معفى · `O` خارج النطاق

الصفري والمعفى وخارج النطاق **بيتطلبوا سبب إعفاء إلزامي** — موجود في
جدول `tax_codes` ومفروض في `TaxCodeController`، والـ seeder فيه الأكواد
المعتادة جاهزة.

## ⚠️ تحذير مهم قبل الإنتاج

`ZatcaSigningService` هو الجزء الأدق تقنيًا في المنظومة كلها:
التوقيع XAdES-BES والتقنين C14N. **فرق بايت واحد** في ترتيب الخصائص
أو المسافات بيفشل التحقق.

**لازم** تختبره ضد أداة التحقق الرسمية (ZATCA SDK / Fatoora validator)
في بيئة `sandbox` ثم `simulation` قبل `production`. متعتبرهوش جاهزًا
للإنتاج قبل ما يعدّي الأداة الرسمية.

وبالأخص `certificateSignature()` (الوسم ٩) — استخراج توقيع الشهادة
محتاج تحليل ASN.1؛ الطريقة العملية إنك تخزّن القيمة اللي بترجع من
الهيئة وقت نداء التوافق وتستخدمها.

## الربط لأول مرة (onboarding)

1. ولّد مفتاح خاص ECDSA و CSR
2. ارفع الـ CSR لمنصة فاتورة واحصل على CSID والسرّ
3. خزّنهم في `company_tax_profiles`:
   `zatca_certificate` · `zatca_private_key` · `zatca_csid` · `zatca_secret`
4. شغّل نداء التوافق على فواتير تجريبية
5. غيّر `zatca_environment` لـ `simulation` ثم `production`

المفتاح الخاص والشهادة مستبعدان من أي ردّ JSON عن طريق `$hidden`
في موديل `CompanyTaxProfile`.

## مهمة مجدولة مقترحة

```php
// app/Console/Kernel.php أو routes/console.php
Schedule::call(function () {
    app(\App\Services\Zatca\ZatcaSubmissionService::class)
        ->retryPending(50);
})->hourly();
```

مهمة للفواتير المبسطة اللي لسه في الطابور والمرفوضة اللي اتصححت —
الإبلاغ ليه مهلة ٢٤ ساعة فالتأجيل مقبول، أما المقاصة فمزامنة.

# منظومة الموارد البشرية — دليل التشغيل

## ١. الهجرات — الترتيب إلزامي

| الملف | ليه |
|---|---|
| `2026_08_05_000001_create_hr_structure_tables` | لازم **قبل** هجرات الحضور (`2026_08_29`) لأنها بتعمل `constrained('hr_employees')` |
| `2026_08_05_000002_create_hr_payroll_engine_tables` | بعد `cost_centers` و`finance_journal_entries` |

الجداول: `hr_branches` · `hr_departments` · `hr_job_titles` · `hr_shifts` ·
`hr_employees` · `hr_employee_contracts` · `gosi_rate_schedules` ·
`hr_leave_types` · `hr_leave_balances` · `hr_leave_requests` ·
`hr_payroll_runs` · `hr_payroll_lines` · `hr_compliance_alerts`

---

## ٢. التشغيل

```bash
php artisan migrate

php artisan db:seed --class=GosiRatesSeeder
php artisan db:seed --class=HrLeaveTypesSeeder
```

ثم أضف في `routes/api.php`:

```php
require __DIR__ . '/hr-routes.php';
```

ومسح الامتثال اليومي في `app/Console/Kernel.php`:

```php
$schedule->call(fn () => app(\App\Services\ComplianceMonitorService::class)->scan())
    ->dailyAt('06:00');
```

---

## ٣. القرارات المعمارية — وليه

### نسب التأمينات بيانات مش كود

`gosi_rate_schedules` بصفوف لها `effective_from` و`effective_to`.
السبب: نسبة التقاعد في النظام الجديد بترتفع **٠.٥٪ على كل طرف كل يوليو
حتى ٢٠٢٨**. أي نظام بيكتب النسب في الكود بيحتاج نشر نسخة جديدة كل سنة.

> ⚠ النسب في الـ seeder من مصادر منشورة بتواريخ ٢٠٢٦ — **راجعها على
> موقع التأمينات الرسمي** قبل التشغيل على بيانات حقيقية. صفوف ٢٠٢٧
> و٢٠٢٨ تقديرية ومعلَّمة كده في الـ `notes`.

### تصنيف الموظف مش بالجنسية

`GosiCalculator::resolveScheme()` بيقرأ `gosi_first_registration_date`
مش `hire_date` ومش الجنسية.

**السبب:** سعودي مُعيَّن جديد عندك في ٢٠٢٦ يبقى على **النظام القديم**
لو عنده سجل اشتراكات سابق قبل ٣ يوليو ٢٠٢٤. استخدام الجنسية وحدها
كمُحدِّد للنسبة = أرقام غلط في المسيّر. ولذلك الجدول فيه عمود
`gosi_scheme` صريح كذلك — القيمة المحفوظة لها الأولوية.

### وعاءان مختلفان

| الحساب | الوعاء | الحد |
|---|---|---|
| **التأمينات** | الأساسي + بدل السكن فقط | ٤٥٬٠٠٠ ريال |
| **نهاية الخدمة** | الأساسي + كل البدلات الثابتة | لا يوجد |

الخطأ في التفرقة بينهم من أشيع أخطاء أنظمة الرواتب.

### مخصص نهاية الخدمة شهريًا

`EosbCalculator::monthlyAccrual()` بيرحّل المخصص **كل شهر** مع الرواتب
بدل مفاجأة التزام كبير وقت الاستحقاق. والقيد بيعمل مدين ودائن على
`2220` — مصروف الشهر والتزامه.

ومعادلة الاستحقاق الكامل (المادة ٨٤): ١٥ يوم لكل سنة في الخمس الأولى،
و٣٠ يوم لكل سنة بعدها. والاستقالة لها نسب مخفّضة (المادة ٨٥):
أقل من سنتين لا شيء، ٢–٥ الثلث، ٥–١٠ الثلثين، وأكثر من ١٠ كامل.

---

## ٤. الميزة على جسر — تكلفة العمالة على المشروع

`PayrollEngine::projectAllocations()`

بصمات المهندس على المشاريع (`project_site_checkins`) بتتحوّل لدقائق،
والدقائق لتكلفة بمعدل `الراتب ÷ (٨ × ٢٢ × ٦٠)`، والنتيجة تُحفظ في
`hr_payroll_lines.project_allocations` كـ:

```json
[{ "project_id": 41, "hours": 62.5, "cost": 4218.75, "share_percent": 48.2 }]
```

**ليه ده مهم:** جسر منصة موارد بشرية، فهو يعرف إن الموظف اشتغل ٨ ساعات
و**مش يعرف على أي مشروع** — وعشان يعرف لازم تكامل خارجي مع الـ ERP.
عندك الاتنين في قاعدة واحدة، فالسؤال «كسبنا كام من المشروع ده بعد
العمالة؟» بقى قابل للإجابة.

---

## ٥. القيد المحاسبي الآلي

`PayrollEngine::post()`

```
مدين   5210  الرواتب والأجور          ← سطر لكل مركز تكلفة
مدين   5230  التأمينات الاجتماعية      (حصة الشركة)
مدين   2220  مخصص نهاية الخدمة
دائن   2130  رواتب مستحقة             (الصافي)
دائن   2150  التأمينات المستحقة        (الحصتين)
دائن   2220  مخصص نهاية الخدمة
```

الحسابات الستة كلها موجودة في `ChartOfAccountsSeeder`.

مصروف الرواتب بيتقسّم **بسطر لكل مركز تكلفة** — فتقرير المصروفات
بالقسم يطلع صح بدون أي شغل إضافي، لأن `hr_departments.cost_center_id`
بيربط القسم بمركزه.

---

## ٦. ملف حماية الأجور — الفحص المسبق

`WpsFileService::validate()` بيفحص قبل التوليد:

| الفحص | النوع |
|---|---|
| آيبان موجود وصحيح (`SA` + ٢٢ خانة) | خطأ يمنع |
| رقم هوية أو إقامة ١٠ أرقام | خطأ يمنع |
| **الراتب في المسيّر يطابق العقد** | خطأ يمنع |
| عقد ساري موجود | خطأ يمنع |
| رقم تأمينات مسجّل | تحذير |
| العقد موثّق في قوى | تحذير |
| صافي الراتب أكبر من صفر | تحذير |

فحص تطابق الراتب هو الأهم — التعارض بين راتب النظام وراتب عقد قوى هو
اللي **بيرفض ملف مدد وبيجمّد خدمات مقيم** عبر السلسلة.

> التكامل المباشر بالـ API محتاج اعتماد رسمي واتفاقيات. توليد الملف
> بالصيغة المطلوبة مع فحص مسبق يعطي ٩٠٪ من القيمة بـ١٠٪ من التعقيد.

---

## ٧. لوحة الامتثال

`ComplianceMonitorService` بيمسح ٨ أنواع:

| النوع | الخطورة | النافذة |
|---|---|---|
| `iqama_expiry` | حرج تحت ٣٠ يوم | ٩٠ يوم |
| `passport_expiry` | تحذير | ١٨٠ يوم |
| `contract_expiry` | حرج داخل مهلة الإشعار | ٦٠ يوم |
| `qiwa_not_authenticated` | حرج للسعوديين | فوري |
| `salary_mismatch` | حرج | فوري |
| `gosi_unregistered` | حرج | فوري |
| `probation_ending` | تحذير | ١٤ يوم |
| `wps_deadline` | حرج تحت ٥ أيام | شهري |

### الرقم اللي بيبيع النظام

`wps_deadline` بيحسب **الغرامة المحتملة بالريال**:
عدد الموظفين × ٣٠٠٠ ريال شهريًا. على ٥٠ موظف ده **١٥٠ ألف ريال في
شهر واحد** — والمخالفة بتأثّر على تقييم نطاقات كذلك.

و`dashboard()` بيرجع `financial_exposure` = إجمالي المخاطرة المفتوحة
بالريال. ده اللي بيحوّل الامتثال من «شغل ورق» لرقم الإدارة بتفهمه.

### نسبة السعودة الحقيقية

`dashboard()` بيرجع رقمين:

- `percentage` — النسبة الظاهرية من عدد السعوديين
- `countable_saudi_employees` — اللي **عقودهم موثّقة في قوى** فعلاً

**السبب:** من ١٥ أبريل ٢٠٢٦، الموظف السعودي لا يُحسب في نطاقات إلا لو
عقده موثّق ومصدَّق إلكترونيًا على قوى. التسجيل في التأمينات وحده
مابقاش كافي. فالنسبة الأولى تفاؤلية والتانية هي الحقيقية.

---

## ٨. الورديات ورمضان

`hr_shifts` فيه `ramadan_start_time` و`ramadan_end_time` و
`ramadan_work_minutes` — تقليل ساعات العمل في رمضان مطلوب نظامًا،
وجسر بيميّز نفسه بده.

وفيه `weekend_days` كـ JSON (`[5,6]` = الجمعة والسبت) و
`grace_minutes` لسماحية التأخير قبل احتسابه.

---

## ٩. الناقص بعد الحزمة دي

- شاشات الفرونت إند للرواتب والامتثال والإجازات
- بوابة الخدمة الذاتية للموظف (موبايل أولًا)
- التوظيف: نظام تتبّع مرشحين + قراءة سير ذاتية
- الأداء: تقييم بمعايير وفترات
- التكامل المباشر بالـ API مع مدد وقوى (يحتاج اعتماد رسمي)
